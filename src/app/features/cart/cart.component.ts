import {CommonModule} from "@angular/common";
import {Component} from "@angular/core";

@Component({
  selector: 'app-cart',
  standalone: true,
  imports: [CommonModule],
  template: `
    <p>
      cart works!
    </p>
  `

})
export class CartComponent {
  // This component is a placeholder for the cart feature.
  // You can add cart-related functionality here in the future.
}
